# Аудит CORE32 — текущее состояние

Состояние зафиксировано 29 июля 2026 года.

Этот файл содержит только актуальные выводы. Промежуточные гипотезы, ранние
варианты исправлений и старые статусы сохранены в истории Git и здесь не
дублируются. Источником точного API служит [`CORE32_API.md`](CORE32_API.md), а
источником распределения физических страниц —
[`PHYSICAL_MEMORY_MAP.ASM`](PHYSICAL_MEMORY_MAP.ASM).

## Текущая сборка

| Артефакт | Размер | SHA-256 |
|---|---:|---|
| `exe/boot.$C` | 31 761 | `1bbcba24831ce7442fa229d1d51e38665447b4bbe64bfbe3ef7738ecf8d16a1a` |
| `Build/boot.payload.bin` | 31 744 | `5c2e4c033c4188145a8a432d09409b16eea8f4a4d279a21439e60feac78c8d87` |
| `Build/CORE32_EXT.bin` | 3 572 | `516a6080623bf542ee47303354732927b41b56af7e9eb3d8e1000ec544d7025a` |
| `Build/CORE32_EXT.CPD` | 2 390 | `bb54e8ded67493eb45290c5359a44c1b4302f225e627018816689633347a3a94` |
| `Build/CORE32T.WMF` | 7 937 | `2d3112fb6233712905808f97f358af45e149e9191c63b562d8aabc3ae823b8d1` |

Штатная `build.ps1` проходит повторно и детерминированно. В
`Build/hash-report.tsv` совпадают 27 runtime-файлов; три ожидаемых несовпадения
с историческим эталоном — улучшенные `boot.$C`, `WC_History.txt` и
`WC_todo.txt`.

Предыдущий Improved до переноса страницы имел SHA-256
`f1234d96552c5b61f4bf3b091472279d5fd24983fd9a8cea5ff1bd82323ee498`,
а его payload —
`74e16029ad6ebba2af29b2bec93e5a504cfcc4556f2977a69f583c1376faa1cd`.
Промежуточная сборка `9c9d4bb5…` отличалась от него ровно двумя байтами:

| Смещение HoBeta | Было | Стало | Назначение |
|---:|---:|---:|---|
| `0x55A8` | `#E0` | `#E8` | страница установки `CORE32_EXT` |
| `0x7162` | `#E0` | `#E8` | страница шлюза `CORE32_EXT` |

Базовая сборка `8e289eb6…` дополнительно изменила APPEND и CORE32T, поэтому
двухбайтовое сравнение относится только к промежуточному переносу страницы и
не описывает ни базовый, ни текущий бинарник.

## Системная карта верхней памяти

| Владелец | Полуинтервал | Страницы |
|---|---:|---|
| TAP/SNA | `[#A1,#C0)` | `#A1..#BF` |
| Mounter RAM-диск | `[#C0,#E8)` | `#C0..#E7` |
| Improved | `[#E8,#F0)` | `#E8..#EF` |
| Commander и система | `[#F0,#100)` | `#F0..#FF` |

`CORE32_EXT` использует `WC_PHYS_CORE32_EXT_PAGE=#E8`. Прежняя страница `#E0`
входила в RAM-диск Mounter и создавала статическую коллизию. CORE32, TAP/SNA и
оба исходника Mounter теперь подключают общую карту. Сборочные `ASSERT`
проверяют смежность диапазонов и запрещают их пересечение.

Системный перенос закрывает E36 на уровне исходников, сборки и упаковки.
Совместный runtime-сценарий «сохранённый RAM-диск Mounter + файловая операция
CORE32» после переноса на `#E8` ещё не выполнен и остаётся обязательной
аппаратной проверкой.

## MKFILE с нулевым размером

### Текущий статус: исправлено

Публичный `MKFILE(0)` теперь сразу создаёт пустой файл и не запускает выделение
места на карте.

Фактический путь:

1. `MKFILE` загружает 32-битный размер в `DE:HL` и вызывает
   `WDOS_EXT.ID_ALLOCATE_FILE`.
2. `ALLOCATE_FILE` проверяет все четыре байта размера.
3. При `DE:HL=0` процедура записывает ноль в `FCTS` и `FCTS+2`, после чего
   возвращает успех.
4. `MKSG` вызывается только для положительного размера.
5. Каталожная запись пустого файла получает `size=0`, `cluster=0`; FAT-цепочка
   не выделяется.

Это проверяет `TEST_EMPTY_FILE` в `tests/core32_unreal/CORE32T.ASM`: после
`MKFILE` и `TENTRY` тест требует нули в `FstClusHI`, `FstClusLO` и 32-битном
размере. Независимая проверка образа требует те же `size=0` и `cluster=0`.

Прямой внутренний `MKSG` рассчитан только на положительный размер. При
`MKSG(0)` уменьшается лишь младшая половина счётчика: вместо нуля получается
`#0000FFFF`, и процедура может начать строить цепочку до 65 536 кластеров.
Поэтому такой вызов запрещён. Официальный плагин должен использовать публичный
`MKFILE`, который при нулевом размере в `MKSG` не входит.

## APPEND и потоковая запись

API 76 APPEND добавляет `BC=0..#4000` байт (до 16 КиБ) в конец файла, найденного последним
успешным `FENTRY`. Реализованы:

- частичный последний сектор через read-modify-write;
- предварительное выделение всей недостающей цепочки без её ранней линковки со
  старым хвостом;
- многоблочная запись полных последовательных секторов внутри кластера;
- обновление FAT с учётом `BPB_ExtFlags`;
- обновление FSInfo;
- публикация нового 32-битного размера последней операцией;
- повторное открытие файла через `FENTRY` и продолжение записи;
- постоянный LBA-кэш текущего кластера с ключом
  `cluster+BSECPC+SDFAT+ADDTOP`; новый `FENTRY` и смена кластера его
  инвалидируют.

В runtime-базисе проверен файл 2 053 байта блоками
`3+509+1+511+512+17+100+400`, пять кластеров и нулевой хвост последнего сектора.
Новый CORE32T также содержит вызов на 16 КиБ, который при `SPC=1` должен
построить цепочку из 32 кластеров. Текущий exact-бинарник прошёл этот сценарий
в патченном Unreal при `SPC=1/8`, с mirrored FAT и active FAT1. Независимая
проверка подтвердила размер 16 384 байта и цепочки из 32 либо четырёх кластеров.

APPEND не является транзакцией: откат выполняется best-effort, а потеря питания
между записями данных, FAT, FSInfo и каталога полностью не закрыта. Усечение,
атомарная замена и прямой 32-битный seek отсутствуют.

## Список найденных проблем E1–E36

Технические слова ниже означают следующее:

- FAT — служебная таблица, в которой карта хранит цепочки частей файла;
- кластер — одна такая часть, состоящая из одного или нескольких секторов;
- FSInfo — служебный сектор со счётчиком свободного места;
- `CF` — флаг процессора, которым драйвер сообщает об ошибке.

Имена в обратных кавычках взяты прямо из исходного кода. Последняя колонка
говорит только о конкретной незавершённой работе, а не повторяет историю
отладки.

| Код | Что было не так | Что сейчас | Что ещё нужно сделать |
|---|---|---|---|
| E1 | Драйвер изменял только первую копию FAT, поэтому резервная копия могла с ней разойтись. | Исправлено. Проверены режим с двумя одинаковыми копиями и режим, в котором рабочей является только вторая копия. | Для каждой новой операции записи в FAT имитировать отказ и проверять согласованность всех её копий. |
| E2 | После удаления файла могли остаться служебные записи его длинного имени. | Исправлено для имени из 20 частей, занимающего три сектора каталога. | Имитировать отказ записи по очереди в каждом из этих трёх секторов и проверить, что каталог не остаётся повреждённым. |
| E3 | Поиск свободного места мог ошибиться при переходе в следующий сектор FAT или на её границе. | Исправлено и проверено на границе двух секторов. | Проверить полный проход до конца FAT, возврат к её началу и отдельный случай, когда свободного места вообще нет. |
| E4 | Некоторые номера конца файловой цепочки распознавались неверно из-за лишних четырёх битов. | Исправлено и проверено на граничных значениях FAT32. | Проверить настоящий файл с очень большим, но допустимым номером кластера. |
| E5 | Счётчик свободного места мог остаться старым после изменения FAT. | Исправлено для исправного, неизвестного и повреждённого значения FSInfo. | Вызвать реальный отказ записи FSInfo и проверить восстановление служебных данных вместе с E15. |
| E6 | Создание пустого файла раньше запускало выделение места и могло начать строить огромную цепочку кластеров. | Исправлено. Пустой файл получает размер 0 и начальный кластер 0; место на карте не выделяется. | Для публичного `MKFILE` ничего. Внутренний `MKSG` нельзя вызывать с нулём; перед его возможной публикацией ему нужен собственный предохранитель. |
| E7 | Было подозрение, что `SRHDRN` берёт номер кластера не из найденной записи каталога. | Подозрение опровергнуто: номер берётся из правильных полей найденной записи. | Ничего. |
| E8 | Старая операция `SAVE512` записывала сектор, но не умела увеличивать файл. | APPEND расширен до 16 КиБ за вызов: недостающая цепочка выделяется заранее, полные секторы записываются многоблочно, размер публикуется один раз. Старый сценарий 2 053 байта и новый максимум 16 КиБ проверены в патченном Unreal при SPC=1/8 и обоих режимах FAT. | Проверить APPEND при отказе записи на каждом этапе восстановления и на реальном устройстве. |
| E9 | Нельзя сразу перейти к произвольному месту в большом файле. | Не исправлено. | Добавить публичную операцию перехода к позиции, заданной 32-битным числом. |
| E10 | Нет безопасной замены старого файла новым: сбой питания может оставить промежуточное состояние. | Не исправлено. | Добавить замену, которую после сбоя можно либо завершить, либо отменить. |
| E11 | Одна из процедур временно меняет общий обработчик `NW0`, поэтому два одновременных обращения к драйверу небезопасны. | Восстановление обработчика исправлено, но одновременно выполнять две файловые операции по-прежнему нельзя. | Убрать изменение общего обработчика или явно разрешать только одну файловую операцию за раз и закрепить это в описании API. |
| E12 | Некоторые драйверы SD-карты могут ждать ответа бесконечно. | В боевых драйверах SD-ZC и DSDNGS проблема не исправлена. | Добавить конечное время ожидания и проверить его на реальном контроллере. |
| E13 | Номер сектора сравнивался с размером FAT как неполное 32-битное число. | Исправлено и проверено на граничных значениях. | Ничего. |
| E14 | `DELFL` позволяет удалить каталог, даже если в нём ещё есть файлы или подкаталоги. | Не исправлено. | Перед удалением разрешать в каталоге только служебные записи `.` и `..`. |
| E15 | Ошибка чтения или записи сектора могла потеряться, и вызывающая программа получала ложный успех. | Код ошибки теперь доходит до вызывающей программы, но уже записанные служебные данные автоматически не восстанавливаются. | Имитировать отказ на каждом этапе и добавить восстановление уже изменённых FAT, FSInfo и записей каталога. |
| E16 | Имя формата 8.3 с пробелом ошибочно считалось обычным коротким именем. | Исправлено для найденного случая. | Добавлять тесты для других сочетаний пробелов, точек и недопустимых символов. |
| E17 | Начальные пробелы в длинном имени по-разному обрабатывались при записи и поиске. | Исправлено для найденного случая. | Каждое новое правило обработки имён закреплять тестами на совпадение и несовпадение. |
| E18 | `SAVE_PREVIOUS_DIR_LBA` портил регистр `HL`. Из-за этого поиск `WC.INI` начинался с неверного адреса и Commander не запускался. | Исправлено и проверено в Unreal. | Проверить, какие регистры меняют остальные вспомогательные процедуры, и закрепить эти правила тестами. |
| E19 | Обычный конец файловой цепочки и ошибка чтения возвращали одинаковый результат. | Исправлено: теперь это два разных результата. | После исправления E12 вызвать настоящий отказ чтения и проверить, что он не воспринимается как нормальный конец файла. |
| E20 | Имя длиной ровно 255 символов ошибочно отвергалось. | Исправлено: 255 символов принимаются, 256 — правильно отвергаются. | Ничего. |
| E21 | Длинное имя из 20 служебных записей неверно восстанавливалось при переходе через три сектора каталога. | Исправлено и проверено для последовательных секторов. | Повторить проверку, когда эти три части каталога лежат в несмежных кластерах. |
| E22 | Шлюз к расширению портил запасной набор регистров `AF'`; из-за этого драйвер мог принять один сектор за 256 и повредить память. | Исправлено и проверено. | Ничего. |
| E23 | Диагностическая обёртка ограничения времени ожидания возвращала неверный номер порта. | В диагностическом драйвере ошибка исправлена; в боевую сборку этот экспериментальный драйвер не входит. | Не возвращать его в боевую сборку до аппаратной проверки исправлений E12 и E32. |
| E24 | Карта без таблицы разделов могла заставить драйвер повторять подключение по кругу. | Исправлено и проверено. | Ничего. |
| E25 | Было подозрение на неверное продвижение по цепочке кластеров в `NOPCLA`. | Отдельной ошибки здесь не найдено. | Отдельная правка не нужна; этот путь проверяется вместе с ошибками чтения и записи из E15. |
| E26 | В полностью заполненном каталоге без завершающей отметки `#00` поиск мог сообщить о ложном совпадении. | Исправлено без изменения фиксированных адресов драйвера. | Ничего. |
| E27 | Общий выход из процедуры мог стереть признак ошибки чтения. | Исправлено и проверено искусственным возвратом ошибки. | После исправления E12 повторить проверку с настоящим отказом контроллера. |
| E28 | Перехват `TENTRY` менял результат, который вернула `FENTRY`. | Исправлено и проверено. | Проверить, сохраняют ли правильный результат остальные перехватчики внутренних операций. |
| E29 | APPEND передавал номер кластера туда, где требовался адрес переменной; запись могла уйти далеко за границы образа карты. | Исправлено. Тесты проверяют адрес сектора, границу тома и неизменный размер образа. | Ничего. |
| E30 | Значение конца цепочки `#FF` заставляло интерфейс F5 повторять операцию бесконечно. | Исправлено: обычный конец снова обозначается `#0F`, а ошибка — отдельно. | Сохранять это различие во всех новых файловых операциях. |
| E31 | При работе с Nemo в окно памяти попадал код подключённого расширения вместо буфера данных. | Исправлено: на время обмена с Nemo страница расширения отключается. Копирование проверено на предыдущем бинарнике. | Повторить копирование через интерфейс WC между Nemo и SD-ZC на текущем бинарнике со страницей `#E8`. |
| E32 | Экспериментальный драйвер SD-ZC не работал на реальном Z-Controller. | Из боевой сборки он удалён; возвращён точный штатный `DOSpSDZC35.CPD`. | Не возвращать экспериментальный драйвер без успешной проверки на настоящем контроллере. |
| E33 | Ошибка, которую сообщает `CURIT`, теряется до следующего шага `GIPAG`. | Не исправлено. | Изменить код: проверять признак ошибки сразу после `CURIT`, затем добавить тест отказа при переходе в следующий кластер. |
| E34 | `DELFL` одинаково сообщает о трёх разных ситуациях: файл не найден, каталог не записан и цепочка кластеров не освобождена. | Не исправлено. | Возвращать отдельный код для каждой из этих ситуаций. |
| E35 | При просмотре каталога конец списка и некоторые ошибки чтения выглядят одинаково. | Не исправлено. | Отделить нормальный конец списка от ошибки, сохранив совместимость со старыми программами. |
| E36 | Расширение CORE32 занимало страницу `#E0`, входившую в память RAM-диска Mounter. Одна программа могла повредить данные другой. | Пересечение устранено в исходниках и проверено сборкой: CORE32 перенесён на страницу `#E8`, а общая карта памяти запрещает повторную коллизию. | На текущем бинарнике заполнить RAM-диск, выполнить файловую операцию CORE32 и убедиться, что данные RAM-диска не изменились. |

## Границы выполненной проверки

Предыдущий exact-бинарник `f1234d96…` прошёл в патченном Unreal:

- SPC=1, mirrored FAT;
- SPC=1, active FAT1;
- SPC=8, mirrored FAT;
- SPC=8, active FAT1;
- CORE32T: основной результат `stage=8 completed=8`;
- LFN-набор: `completed=10`;
- штатные UI-циклы F5/F8 на SD-ZC и между SD-ZC/Nemo;
- пустой файл, SFN/LFN, 255/256 символов, границы FAT, FSInfo, APPEND и
  искусственный возврат потоковой ошибки.

Промежуточный exact-бинарник `9c9d4bb5…` отличался от этого runtime-базиса
только двумя операндами выбора страницы `#E0 → #E8`. Для него подтверждены:

- повторяемая сборка;
- совпадение 29 остальных runtime-файлов;
- обратная распаковка `CORE32_EXT`;
- побайтовое совпадение payload внутри HoBeta;
- инъекция в `core32_e8_mirrored.img`;
- C1–C4 для образа;
- успешная ROM-загрузка на реальном устройстве.

Совместный сценарий Mounter + CORE32 после переноса страницы ещё не повторялся.
Результаты предыдущего exact-бинарника не выдаются за аппаратную проверку
текущей сборки.

Базовый exact-бинарник `8e289eb6…` дополнительно содержит APPEND до 16 КиБ.
Для него подтверждены полная `build.ps1`, обратная распаковка расширения,
совпадение остальных 29 runtime-файлов и четыре актуальных CORE32T-прогона:
`SPC=1/8`, mirrored FAT и active FAT1. Во всех режимах окно показало
`CORE32 TEST PASS`; после штатного закрытия Unreal независимая проверка получила
основной цикл `8/8`, LFN-цикл `10/10`, размер `APP16K.BIN` 16 384 байта и
цепочки из 32 либо четырёх кластеров. Проверка на реальном устройстве ещё не
выполнялась.

Проверенный CORE32-базис `55dfe646…` добавляет межвызовный LBA-кэш. Подтверждены
полная `build.ps1`, обратная распаковка, единственное ожидаемое отличие
`boot.$C` от комплекта и машинный тест кэша: первый промах, межвызовное
попадание без `GIPAG`, промахи после смены кластера, геометрии FAT и начала
раздела. Полный CORE32T прошёл в четырёх режимах `SPC=1/8`, mirrored FAT и
active FAT1: основной цикл `8/8` с `append_gipag=1`, LFN-цикл `10/10`,
`APP16K.BIN` имеет размер 16 384 байта и цепочку из 32 либо четырёх кластеров.
После экранного `CORE32 TEST PASS` Unreal закрывался штатной командой
`File → Exit`; закрытие вспомогательного GDI+ окна не фиксирует последний
буфер HDD-образа и не считается корректным завершением теста. UI-циклы F5/F8
и реальное устройство для этой сборки ещё не проверялись.

Текущий дистрибутивный бинарник `1bbcba24…` меняет только код Commander до
`#C000`: файловая часть от `#C000` до конца payload побайтно совпадает с
`55dfe646…`. Для текущего бинарника повторно прошли полная сборка и машинный
тест LBA-кэша; четыре CORE32T-прогона выше относятся к побайтно сохранённой
файловой части и не выдаются за новый UI-прогон полного бинарника.

Независимо проверенные образы в `C:\Users\Администратор\Desktop\Unreal`:

| Режим | Образ | SHA-256 |
|---|---|---|
| SPC=1, mirrored FAT | `core32_lbacache_spc1_mirrored.img` | `1ae1829da50ba6b1cc529bce08399144ef36bd76ca2624025d70e4fa3b46f42b` |
| SPC=1, active FAT1 | `core32_lbacache_spc1_active1.img` | `7d841bc5eefa7c0011360c956a9b33f5c765f56786488d0781d3d7d861bb823b` |
| SPC=8, mirrored FAT | `core32_lbacache_spc8_mirrored.img` | `70026d805ce5cba95f2aaee8eff098b4bd2c42d8ab095adbcab3ee736e1b8359` |
| SPC=8, active FAT1 | `core32_lbacache_spc8_active1.img` | `15280a82ace7d168a6702106f967cdc6796f009c5da6f77c383c583fd189992d` |

Образ `core32_e8_mirrored.img` имеет SHA-256
`76d484e199aa1da334d34f248e5f30b2bad82ef95df6f12029f1cc860945f624`.
ROM сначала не нашёл `BOOT.$C` после простой перезаписи старого файла. Рабочая
процедура обновления: удалить старый `BOOT.$C`, записать новый, безопасно
извлечь носитель. После удаления и новой записи загрузка прошла.

## Открытые действия

1. Выполнить совместный runtime-тест Mounter RAM-диска и файловой операции
   CORE32 на странице `#E8`.
2. Исправить E33: не терять `CF` после `CURIT` перед `GIPAG`.
3. Исправить E14/E34/E35: безопасное удаление непустого каталога и однозначные
   ошибки каталожного API.
4. Спроектировать E9/E10: прямой seek и атомарную замену.
5. Убрать остаточную нереентерабельность E11.
6. Перепроектировать конечные ожидания SD-ZC/DSDNGS и проверить их на реальном
   контроллере до возврата в боевой boot.
7. Добавить физические отказные тесты FAT, FSInfo, LFN и APPEND, включая
   транзакционный откат E15.

## Файлы-источники

| Файл | Назначение |
|---|---|
| [`CORE32.ASM`](CORE32.ASM) | Основные файловые процедуры и шлюз расширения |
| [`CORE32_EXT.ASM`](CORE32_EXT.ASM) | Исправления FAT/LFN/FSInfo, APPEND и приватный runtime |
| [`CORE32_EXT_API.ASM`](CORE32_EXT_API.ASM) | ID приватного шлюза |
| [`CORE32_API.md`](CORE32_API.md) | Публичный и внутренний ABI |
| [`PHYSICAL_MEMORY_MAP.ASM`](PHYSICAL_MEMORY_MAP.ASM) | Единственная карта верхних физических страниц |
| [`../tests/core32_unreal/CORE32T.ASM`](../tests/core32_unreal/CORE32T.ASM) | Runtime-регрессия |
| [`../tests/core32_unreal/test_append_lba_cache.py`](../tests/core32_unreal/test_append_lba_cache.py) | Машинный unit-тест постоянного LBA-кэша |
| [`../tests/core32_unreal/core32_image_test.py`](../tests/core32_unreal/core32_image_test.py) | Подготовка и независимая проверка образов |
| [`../build.ps1`](../build.ps1) | Воспроизводимая двухпроходная сборка и hash-аудит |
