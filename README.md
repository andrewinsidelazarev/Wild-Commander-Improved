# Wild Commander Improved

**Wild Commander Improved v1.10i 2026-08-28** — улучшенная сборка двухпанельного файлового
менеджера Wild Commander для ZX Evolution/TS-Conf. Интерфейс, горячие клавиши,
конфигурация и существующие плагины остались привычными. Основные изменения
сделаны внутри FAT32-ядра, чтобы надёжнее копировать, создавать и удалять файлы
на SD-картах и IDE-накопителях.

Сам Commander позволяет работать с файлами и каталогами в двух панелях,
запускать `$C`, SPG и WMF, просматривать и редактировать файлы и подключать
плагины для архивов, образов, графики, музыки и сервисных задач. Этот репозиторий
содержит не только дизассемблированный исходник, но и готовое обновление,
воспроизводимую сборку, тесты и документацию API.

**Быстрые ссылки:** [готовый `boot.$C`](exe/boot.$C) ·
[ChkDsk FAT32 v0.06](exe/WC/CHKDSK.WMF) ·
[справочник CORE32 API](source/CORE32_API.md) ·
[аудит и ограничения](source/CORE32_audit.md).

## Зачем обновляться

Старая версия могла неверно работать с некоторыми штатными ситуациями FAT32.
Обновление исправляет конкретные причины повреждения или потери данных:

- обе копии FAT теперь обновляются согласованно, а на носителях с одной активной
  FAT используется именно выбранная копия;
- пустые файлы создаются без ошибочной цепочки кластеров размером до 4 ГиБ;
- длинные имена удаляются целиком, без оставшихся записей-«призраков»;
- конец цепочки файлов и каталогов распознаётся корректно, без перехода по
  несуществующим кластерам;
- найденная позиция свободного кластера сохраняется в RAM текущего сеанса;
  обычный `MKFILE` не выполняет отдельную запись FSInfo на SD-ZC;
- сообщённая драйвером ошибка чтения или записи останавливает файловую операцию,
  вместо продолжения с уже неверными данными;
- плагины получили API 76 APPEND: можно дописать до 16 КиБ в конец файла;
  недостающая FAT-цепочка выделяется заранее, полные секторы записываются
  многоблочно, а новый 32-битный размер публикуется один раз в конце вызова;
- последовательные APPEND внутри одного кластера сохраняют вычисленный базовый
  LBA; ключ из кластера, геометрии FAT и начала раздела исключает повторное
  использование устаревшей позиции;
- добавлен API 77 `FILEX`: прямые `READ_AT`/`WRITE_AT`, 32-битный
  `SET_EOF32`, сведения о томе, безопасный move/rename между каталогами и
  FAT metadata. Рост заранее проверяет реальные data-кластеры и на полном
  диске не публикует ложный размер;
- приватное расширение CORE32 перенесено из занятой Mounter страницы `#E0` в
  зарезервированный диапазон Improved `#E8..#EF`; общая карта верхней памяти
  теперь проверяется при сборке;
- добавлен тип плагинов `#06`: такие плагины из секции `[PLUGINS]` один раз
  запускаются автоматически при старте WC, после GEDZ и DEF, но перед
  восстановлением каталогов и позиций панелей через PATH. Типы `#01` и `#04`
  сохранили прежнее назначение. После выполнения заголовок `#06` удаляется из
  обычной таблицы, поэтому такой плагин не появляется в пользовательском меню.
- в комплект добавлен `CHKDSK.WMF v0.06`: его страничная BFS-очередь вмещает
  8191 ожидающий каталог; широкий тест на 6740 непосредственных подкаталогах
  прошёл обычную и глубокую проверки в патченном Unreal без ошибок.

Это обновление надёжности файловых операций, а не новый интерфейс. Оно особенно
полезно тем, кто регулярно копирует и удаляет данные на FAT32, использует
длинные имена или плагины, создающие файлы.

## Как обновить v1.10 до v1.10i

1. Сделайте полный образ важного носителя и отдельно сохраните текущий
   `boot.$C` на ПК.
2. Удалите старый `boot.$C` с носителя. Не обновляйте его простой перезаписью:
   на реальном устройстве после такой операции ROM сообщил, что файл не найден.
3. Запишите [новый `exe/boot.$C`](exe/boot.$C), безопасно извлеките носитель и
   только затем включите устройство. Остальные старые плагины и `WC/wc.mnu`
   заменять не нужно.
4. Для API 77 скопируйте [FILEX.WMF](exe/WC/FILEX.WMF) в каталог `WC` и
   добавьте `FILEX.WMF` первой строкой секции `[PLUGINS]` своего `WC/wc.ini`.
   Готовый пример уже находится в [wc.ini](exe/WC/wc.ini); пользовательские
   настройки панелей лучше перенести вручную, а не затирать целиком.
5. Для проверки FAT32 скопируйте [CHKDSK.WMF](exe/WC/CHKDSK.WMF) в каталог
   `WC` и добавьте `CHKDSK.WMF` в секцию `[PLUGINS]`. В комплектном
   [wc.ini](exe/WC/wc.ini) эта строка уже стоит после `UNZIP.WMF`.
6. После запуска убедитесь, что заголовок показывает `Wild Commander v1.10i`,
   затем проверьте копирование и удаление ненужного тестового файла.
7. Для отката удалите новый файл и таким же способом запишите сохранённый старый
   `boot.$C`: миграции данных или
   конфигурации нет.

В комплекте v1.10 с этой сборкой побайтно совпадают 25 прежних runtime-файлов.
Намеренно отличаются `boot.$C`, `WC/wc.ini`, `WC_History.txt`, `WC_todo.txt`, а
`WC/FILEX.WMF`, `WC/UNZIP.WMF` и `WC/CHKDSK.WMF` являются новыми файлами.
`Build/CORE32_EXT.bin`,
`Build/CORE32_EXT.CPD`, `CORE32T.WMF`, `FILEXT.WMF` и `FILEXNST.WMF` на носитель
копировать не нужно: это встроенные части или тестовые артефакты.

Обновление со старых и изменённых комплектов отдельно не проверялось. Для них
сначала сохраните всю установленную систему и пользовательские настройки;
совместимость гарантируется только с комплектными v1.10 `wc.ini` и плагинами.

## Кому пока лучше подождать

Если нет резервной копии либо система должна гарантированно переживать отказ
контроллера во время записи, сначала испытайте сборку на запасном носителе.
Предыдущие exact-бинарники проверялись в патченном Unreal на SD-ZC и Nemo, а
одна предшествующая сборка со страницей `CORE32_EXT=#E8` загружалась через ROM
на реальном устройстве. Текущий `58f78087…` прошёл FILEX-набор и полную матрицу
CORE32 в Unreal на `SPC=1/8`, mirrored FAT и active FAT1: все семь операций,
SHA-256 после нового открытия и инъекции отказов каталога, metadata, FAT и
FSInfo. Отдельный полностью заполненный образ вернул `NO_SPACE=#22` без ложного
размера, расширения образа и orphan-кластеров. Исторический exact-файл от
5 августа 2026 года был записан в корень SD-карты EVO и отдельным чтением
побайтно сверён с локальной сборкой, но это подтверждает только доставку того
старого файла. Текущий exact-бинарник на реальном ZX Evolution не проверен.

В боевой сборке сохранён точный штатный packed-драйвер SD-ZC без runtime-патча.
Аппаратная проверка показала, что подготовка `CMD55+ACMD23` не ускоряет первый
COPYF, поэтому она удалена. При монтировании `FSI_Nxt_Free` принимается только
после проверки диапазона и соответствующей записи FAT. Для занятого,
неизвестного или недопустимого значения используется read-only RAM-курсор в
точке 15/16 области данных; штатный циклический обход при необходимости доходит
до конца и один раз возвращается к кластеру 2. Обычный `MKFILE` не записывает
FSInfo автоматически: дополнительный I/O после создания FAT-цепочки зависал на
реальной SD-ZC как в F5 COPY, так и при сохранении TXTEDIT. Найденная позиция
остаётся в RAM текущего сеанса.
Низкоуровневые ожидания драйвера всё ещё могут зависнуть, если карта или
контроллер перестанут отвечать: ограничение E12 не закрыто.

## Документация для разработчиков

- [Полное руководство программиста](docs/README.md) — архитектура панелей,
  плагины, весь публичный API `0..86`, драйверы, все публичные и внутренние
  вызовы WildDOS/CORE32 и карта памяти;
- [единая PDF-версия руководства](docs/Wild_Commander_Improved_Programmer_Guide.pdf) —
  оглавление, таблицы, схемы и готовые примеры ASM для начинающего программиста;
- [Цельный справочник CORE32 API](source/CORE32_API.md) — публичные вызовы,
  регистры, флаги, API 76 APPEND, API 77 FILEX и внутренний интерфейс
  `CORE32_EXT`;
- [Машиночитаемый ABI FILEX](source/CORE32_FILEX_API.ASM) — 32-байтовый блок,
  операции, выходные структуры и стабильные статусы;
- [Аудит CORE32](source/CORE32_audit.md) — причины ошибок, реализованные
  исправления, тестовая матрица и оставшиеся ограничения;
- [Приватные ID шлюза CORE32_EXT](source/CORE32_EXT_API.ASM) — интерфейс между
  основной и расширенной частями ядра;
- [Карта физических страниц](source/PHYSICAL_MEMORY_MAP.ASM) — единый
  исполняемый контракт TAP/SNA, Mounter, Improved и Commander;
- [Типы плагинов](source/plugins/READ_ME.TXT) — формат заголовка и назначения
  типов, включая автозапуск `#06`;
- [Исторический C-заголовок API плагинов](source/plugins/TRDisp/src/wc_api.h) —
  объявления для существующих C-плагинов; при расхождении источником истины
  является цельный справочник выше.

## Проверенная сборка

Базовый CORE32 ранее прошёл четыре режима `SPC=1/8`, mirrored FAT и active
FAT1, включая APPEND 16 КиБ и межвызовный LBA-кэш. Для текущего exact-бинарника
дополнительно выполнены два полных FILEX-прогона в патченном Unreal:
`SPC=1/mirrored` и `SPC=8/active FAT1`. В обоих завершены все семь тестовых
групп, позиционное чтение фрагментированного диапазона потребовало четыре
низкоуровневых чтения, а отказы каталога, metadata, FAT и FSInfo вернули
`MEDIA=#21`. После штатного закрытия и нового открытия образа проверены размер,
байты, FAT-цепочки и SHA-256.

Третий образ был физически заполнен до `free=0`. Рост пустого файла вернул
`NO_SPACE=#22`, оставил `size=0/cluster=0`, не увеличил размер образа и не
создал orphan-кластеров. Машинные unit-тесты отдельно проверяют LBA-кэш,
удаление заголовков типа `#06` из меню и фиксированный ABI FILEX. Для текущего
exact-бинарника в Unreal также прошли плоские F5/F6 и рекурсивные F5/F8 с
точными байтами, равными FAT и без orphan-кластеров. Запуск этого кандидата на
реальном ZX Evolution не проверен.

Расширение `CURRENT_DIR+REPLACE` дополнительно прошло полный FILEXT-прогон на
существующем `SPC=1/mirrored` образе. Отдельный UI-прогон на уже существующем
`SPC=8/mirrored` образе использовал точную 541-байтовую копию рабочего
`wc.ini`: редактор записал только два логических сектора, сохранил однобайтовое
изменение и вернулся на панели за 251 мс. После штатного выхода новое открытие
образа подтвердило точные байты, равные FAT, отсутствие служебных `WCE*` и
orphan-кластеров. В обычном FILEX-пути создание temp сразу выполняет `MKFILE`;
два предварительных просмотра каталога удалены, а занятые recovery-имена
по-прежнему не перезаписываются.

Штатный F5 отдельно проверен для локального `boot.$C` `f5406678…`. Машинный
Z80-тест подтверждает 32-битный остаток и точные значения `B`: файл 541 байт
вызывает по два сектора `LOAD512` и `SAVE512`, а нулевой файл не вызывает их.
UI-прогон использовал уже существующий `core32_spc8_diag_live2.img`: копирование
541 байта завершилось между `20:58.03` и `20:58.30` на часах WC. После штатного
`File → Exit` новое открытие подтвердило точный SHA-256, размер 541 байт, одну
SPC8-цепочку, равные FAT, отсутствие orphan-кластеров и неизменный `wc.ini`.
Это проверка в Unreal, не аппаратное подтверждение EVO.

Контрольные артефакты:

- payload: 31 744 байта, SHA-256
  `16f9220d1da9be25eabc9660e7885abec768bc71c2aebe5f8e9de71476d85334`;
- HoBeta [`exe/boot.$C`](exe/boot.$C): 31 761 байт, SHA-256
  `7474e8044e8615547bea6442b049fcdda157aa9fb0f36f848c19cd9ef0a98cfe`;
- `Build/CORE32_EXT.bin`: 3 596 байт, SHA-256
  `c0b436f6479b44a6bcfc3c9803bba7433c30e7537db73bcb5fe36e3b8e345244`;
- [`exe/WC/FILEX.WMF`](exe/WC/FILEX.WMF): 7 202 байта, SHA-256
  `3d30486ab3710d27d1e766462aafb0a0f1a0e2179ff1d451dc948092d5ac5959`;
- [`exe/WC/TXTEDIT.WMF`](exe/WC/TXTEDIT.WMF): 10 235 байт, SHA-256
  `3961b8ecbc4d58b57efb8a6d8032b8ba2affc33bb71e4256f5af2e8056e67035`;
- [`exe/WC/CHKDSK.WMF`](exe/WC/CHKDSK.WMF): 11 599 байт, SHA-256
  `e74d5f21760fae0c09e84d2e9e7095613c67f79b2c8fd4a801e168822c631216`;
- `Build/FILEXT.WMF`: 4 568 байт, SHA-256
  `a65f29cd6d8a0c18f0ab71705f38eced523994b7850dc7f0729a22faeb633875`;
- `Build/FILEXNST.WMF`: 1 424 байта, SHA-256
  `32c21d6aa5f69401cd4d5547c3cdab4ebcdd7928e9e48ad454e4f69afb1f4d94`.

`CHKDSK.WMF` собран из отдельного проекта
[Chkdsk-ts-config-wc-plugin](https://github.com/andrewinsidelazarev/Chkdsk-ts-config-wc-plugin)
на коммите `17e3a9bc53edcdc3d8169f75124acdf9be9c9bba`; `build.ps1` проверяет его
версию, число страниц и точный SHA-256 перед включением в комплект.

`source/BOOT.ASM` — точка побайтно точной исследованной сборки
`boot.$C` из восстановленных исходников и оставшихся exact-модулей. Код разделён
по исходным подсистемам `MD20`, `PS2P`, `WCIF`,
`WCVLS`, `WCFX`, `WCVW`, `WCINI`. WildDOS восстановлен как осмысленные
`CORE32.ASM` и `KERNEL.ASM`, четыре точных упакованных драйвера и исходный
распаковщик `DEHR2.ASM`.

Все модули лежат рядом в `source`. Исторический baseline собирался SJASMPlus
1.21 побайтно точно; активный улучшенный `boot.$C` намеренно отличается:

- неизменённые прежние runtime-файлы: 25 `MATCH`;
- намеренно изменены `boot.$C`, `WC/wc.ini`, `WC_History.txt`, `WC_todo.txt`;
  новые `WC/FILEX.WMF`, `WC/UNZIP.WMF` и `WC/CHKDSK.WMF` отмечены как
  `EXTRA_ACTUAL`, а `WC/TXTEDIT.WMF` — как намеренно изменённый.

Рабочие исследованные файлы имеют синтаксис SJASMPlus, кодировку UTF-8 без BOM
и русские комментарии о назначении подсистем и основных процедур. Исходники
плагинов находятся прямо в `source/plugins`. Вытесненный линейный дизасм и
прочие кандидаты на удаление изолированы в `source/to delete` и не участвуют
в сборке.

Где исторический ASM доказанно совпадает с эталоном, используется именно он:
`WCVLS.ASM`, `WCFX.ASM`, `WCVW.ASM`, `WCINI.ASM`, `DEHR2.ASM` и четыре
драйвера. В `WCFX` и `WCVW` старые цепочки повторов явно развёрнуты в синтаксис
SJASMPlus. Для `MD20`, `PS2P` и `WCIF` локальные исторические версии отличаются
от поставляемого бинарника, поэтому их точные исследованные версии теперь
являются единственными активными файлами `MD20.ASM`, `PS2P.ASM`, `WCIF.ASM`.

Сборка:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1
```

Сценарий ассемблирует Commander из `BOOT.ASM`, упаковывает HoBeta, сохраняет
собственные `WC_History.txt` и `WC_todo.txt`, размещает остальные локальные
runtime-файлы плагинов, меню и конфигурации и проверяет всё дерево `exe`
относительно локального эталона. Отчёт: `Build/hash-report.tsv`.

Для активной улучшенной сборки успешный результат — 25 совпадений и восемь
ожидаемых отличий: изменённые `boot.$C`, `WC/wc.ini`, `WC_History.txt`,
`WC_todo.txt`, `WC/FILEX.WMF`, `WC/TXTEDIT.WMF`, `WC/UNZIP.WMF` и
`WC/CHKDSK.WMF`. Ключ
`-RequireExact` сохраняет строгий режим, где любое отличие от исторического
эталона является ошибкой.

В эту же сборку входит машинный `test_wcfx_copy_bounds.py`: он проверяет
нулевой файл, границы 511/512/513 и 16383/16384/16385 байт, 32-битный остаток,
точные значения `B` для `LOAD512/SAVE512` и сохранение связанного адреса
`WCFX.ERR0` в WCINI. Эта же проверка закрепляет выдачу NXTETY для F5 COPY,
F6 RENAME и рекурсивных F5/F8: служебное 8.3-дополнение удаляется у расширений
длиной ноль, один или два символа, а LFN с расширением произвольной длины не
изменяется. Скрипт `wcfx_copy_unreal.py` отдельно готовит и проверяет на уже
существующем рабочем FAT32-образе как плоские F5/F6, так и рекурсивные F5/F8
по вложенному непустому дереву глубиной 21 уровень; новые образы он не создаёт.

Перед основной сборкой также заново ассемблируются `DIDENEMO`, `DIDESMUC`,
`DSDZC` и `DSDNGS`; размеры и SHA-256 их распакованных образов проверяются
против драйверов, извлечённых из WildDOS.

Точные Hrust-потоки NEMO/SMUC сохранены в исходных CPD, а для ZC/NGS — в
минимальных `DOSpSDZC35.CPD` и `DOSpSDNGS35.CPD`. Доступный локально MHMT
создаёт совместимые, но побайтно другие потоки, поэтому эти два блока пока
остаются двоичными ресурсами. Исследование упаковки: `Build/research/driver_pack/REPORT_RU.md`.

Исправленный SD-ZC собирается и упаковывается как диагностический артефакт, но
в боевой `boot.$C` после аппаратного регресса входит точный штатный
`DOSpSDZC35.CPD` без наложения кода после распаковки. Низкоуровневые тайм-ауты
SD-ZC остаются открытым пунктом.

Точные границы подсистем закреплены директивами `ASSERT`. Скрипты из `tools`
служат только воспроизводимой технической проверкой и не являются исследуемым
исходным кодом Commander.
